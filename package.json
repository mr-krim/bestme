{
  "name": "bestme",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd ui && npm run dev",
    "build": "cd ui && npm run build",
    "tauri": "cargo tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
} 
